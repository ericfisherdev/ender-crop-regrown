org.gradle.jvmargs=-Xmx3G
org.gradle.caching=true
org.gradle.parallel=true

# Archive
archives_base_name=endercrop-regrown
mod_version=2026.5.15.0
group=be.mathiasdejong.endercrop

# Minecraft / NeoForge
minecraft_version=26.1.2
neoforge_version=26.1.2.55-beta

# Parchment has no MC 26.1.x mappings (latest published is parchment-1.21.11).
# The build stays Mojmap-only until a parchment-26.1.x set ships.

# Compat integration versions — researched and pinned in Phase 1 (2026-05-15).
# Wired into build.gradle's dependencies block in Phase 4 (compat rewire); kept
# as properties here so the pins live in one place.
#   Available for 26.1.2:
# cloth_config_version=26.1.154   # me.shedaniel.cloth:cloth-config-neoforge
# fcap_version=26.1.4             # fuzs.forgeconfigapiport:forgeconfigapiport-neoforge
# jade_file_id=8068368            # curse.maven:jade-324717 (Modrinth 26.1.1+neoforge)
#   No 26.1.x release exists yet (integration deferred — see plan R4/R9):
# theoneprobe_version=            # latest is 1.21_neo-12.0.8 (MC 1.21.1 only)
# botanypots: no 26.1.x NeoForge build; latest is 21.1.41 (MC 1.21.1 only)

# Mod properties
mod_id=endercrop
mod_name=Ender Crop
mod_author=DrManganese
mod_licence=GPL-3.0-only
mod_description=Adds an ender pearl crop and tilled end stone to grow it on.
mod_url=https://github.com/ericfisherdev/ender-crop-regrown
